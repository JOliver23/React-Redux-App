const intitialState = {
    quote: '',
    isFetching: false,
    error: ''
}

export const quotesReducer = (state = intitialState, action) => {
    switch(action.type) {
        default:
            return state;
    }
};